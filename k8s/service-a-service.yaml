apiVersion: v1
kind: Service
metadata:
  name: service-a
  namespace: microservices
  labels:
    app: microservice
spec:
  selector:
    app: service-a
  ports:
  - name: http
    port: 80
    targetPort: 5000
