apiVersion: v1
kind: Service
metadata:
  name: service-b
  namespace: microservices
  labels:
    app: service-b
    monitoring: enabled
spec:
  selector:
    app: service-b
  ports:
  - name: http
    port: 80
    targetPort: 5000
